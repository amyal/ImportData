{% extends '::layout.html.twig' %}

{% block main_container_header %}
    <i class="icon-building"></i> {{ 'organisationRepositories.list.title'|trans }}
{% endblock %}

{% block breadcrumb %}
    <div class="breadcrumb-button">
        <span class="breadcrumb-label">{{ 'configuration.nav'|trans }}</span>
        <span class="breadcrumb-arrow"><span></span></span>
    </div>
    <div class="breadcrumb-button">
        <span class="breadcrumb-label">{{ 'organisationRepositories.list.title'|trans }}</span>
        <span class="breadcrumb-arrow"><span></span></span>
    </div>
{% endblock %}

{% block content %}
    <div class="container-fluid padded">
        <div class="row-fluid">
            <div class="span12">
                <div class="box">
                    <div class="box-header">
                        <span class="title"> {{ 'organisationRepositories.list.title'|trans }} </span>
                        <ul class="box-toolbar">
                            <li><a class="btn btn-green btn-mini spacer" href="{{ path('srm_indicator_organisation_repositories_add', { 'organisationId': organisation.organisationId }) }}">{{ 'button.add'|trans }}</a>
                                <!--<a class="btn btn-green btn-mini">Exporter</a>-->
                            </li>
                        </ul>
                    </div>
                    <div class="box-content">
                        <table class="table table-normal">
                            <thead>
                                <tr><td>{{ 'organisationRepositories.list.name'|trans }}</td>
                                    <td>{{ 'organisationRepositories.list.organisation'|trans }}</td>
                                    <td>{{ 'list.enabled'|trans }}</td>
                                    <td style="width:115px"></td>
                                </tr>
                            </thead>
                            <tbody>
                                {% for organisationRepository in organisationRepositories %}
                                    <tr>
                                        <td>{{ organisationRepository.label }}</td>
                                        <td>{{ organisationRepository.organisation.label }}</td>
                                        <td>
                                            {% if organisationRepository.enabled %}
                                                {{ 'enabled'|trans }}
                                            {% else %}
                                                {{ 'disabled'|trans }}
                                            {% endif %}
                                        </td>
                                        <td>
                                            <div class="btn-group">
                                                <a href="{{ path('srm_indicator_organisation_repositories_show', { 'organisationId': organisation.organisationId, 'organisationRepositoryId': organisationRepository.organisationRepositoryId }) }}" class="btn btn-default" title="{{ 'button.show'|trans }}"><i class="icon-eye-open"></i></a>
                                                <a href="{{ path('srm_indicator_organisation_repositories_edit', { 'organisationId': organisation.organisationId, 'organisationRepositoryId': organisationRepository.organisationRepositoryId }) }}" class="btn btn-default" title="{{ 'button.edit'|trans }}"><i class="icon-pencil"></i></a>
                                                <a href="{{ path('srm_indicator_organisation_repositories_disable', { 'organisationId': organisation.organisationId, 'organisationRepositoryId': organisationRepository.organisationRepositoryId }) }}" class="btn btn-danger" title="{{ 'button.delete'|trans }}"><i class="icon-trash"></i></a>
                                            </div>
                                        </td>
                                    </tr>
                                {% else %}
                                    <tr class="info"><td colspan="9">{{ 'organisationRepositories.list.empty'|trans }}</td></tr>
                                {% endfor %}
                            </tbody>
                        </table>

                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
