{% extends '::layout.html.twig' %}

{% block main_container_header %}
    <i class="icon-circle-blank"></i> {{ 'indicators.list.title'|trans }}
{% endblock %}

{% block breadcrumb %}
    <div class="breadcrumb-button">
        <span class="breadcrumb-label">{{ 'indicator.nav'|trans }}</span>
        <span class="breadcrumb-arrow"><span></span></span>
    </div>
    <div class="breadcrumb-button">
        <span class="breadcrumb-label">{{ 'indicators.nav.control'|trans }}</span>
        <span class="breadcrumb-arrow"><span></span></span>
    </div>
{% endblock %}

{% block content %}
    <div class="container-fluid padded">
        <div class="row-fluid">
            <div class="span10 offset1">
                <div class="box">
                    <div class="box-header">
                        <span class="title"> {{ 'indicators.list.title'|trans }} </span>
                    </div>

                    {{ form_start(form, {'attr': {'class': 'form-horizontal fill-up'}}) }}
                        {{ form_errors(form) }}

                        <div class="box-content padded">
                            <div class="padded-box">
                                <div class="control-group">
                                    {{ form_label(form.label, 'Volume d\'eau de surface prélevée (y compris zones humides, rivières, lacs, océans)') }}
                                    <div class="controls"><input id="srm_indicator_label" type="text" value="114" autofocus="autofocus" required="required" name="srm_indicator[label]">&nbsp;m3</div>
                                </div>
                                <div class="control-group">
                                    {{ form_label(form.label, 'Volume d\'eau prélevée dans la nappe phréatique') }}
                                    <div class="controls"><input id="srm_indicator_label" type="text" value="166" autofocus="autofocus" required="required" name="srm_indicator[label]">&nbsp;m3</div>
                                </div>
                                <div class="control-group">
                                    {{ form_label(form.label, 'Volume d\'eau pluviale collectée et stockée directement par l’organisation') }}
                                    <div class="controls"><input id="srm_indicator_label" type="text" value="75" autofocus="autofocus" required="required" name="srm_indicator[label]">&nbsp;m3</div>
                                </div>
                                <div class="control-group">
                                    {{ form_label(form.label, 'Volume d\'eaux usées collectées par une autre organisation') }}
                                    <div class="controls"><input id="srm_indicator_label" type="text" value="0" autofocus="autofocus" required="required" name="srm_indicator[label]">&nbsp;m3</div>
                                </div>
                                <div class="control-group">
                                    {{ form_label(form.label, 'Volume d\'eau de ville ou d\'autres sources d\'approvisionnement en eau') }}
                                    <div class="controls"><input id="srm_indicator_label" type="text" value="1.670" autofocus="autofocus" required="required" name="srm_indicator[label]">&nbsp;m3</div>
                                </div>
                            </div>
                        </div>

                        {{ include('SrmWebsiteBundle:Partials:_save_cancel.html.twig', {
                            save_form: form.save,
                            url_cancel: path('srm_indicator_indicator_list', {'organisationId': organisationId, 'repositoryId': repositoryId})
                        }, with_context = false) }}

                        <div class="hide">
                            {{ form_widget(form._token) }}
                            {{ form_rest(form) }}
                        </div>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}